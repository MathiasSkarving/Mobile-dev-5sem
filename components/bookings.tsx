import {Button, Text} from 'react-native-paper'
import {View} from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context';

export default function Bookings() {
    return (
        <SafeAreaView>
            <Text >These are your bookings:</Text>
        </SafeAreaView>
    );
}